# GenerateProjectContext-PS
PowerShell tool to generate project file structure and code content into a Markdown file for LLM context." (PowerShell製のツールで、プロジェクトのファイル構造とコード内容をLLMコンテキスト用のMarkdownファイルに生成します。
